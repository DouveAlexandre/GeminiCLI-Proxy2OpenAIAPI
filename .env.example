# Port where the server will run
PORT=11434

# Authentication type - can be 'oauth-personal', 'gemini-api-key', or 'vertex-ai'
# Use oauth-personal for free access to Gemini 2.5 Pro by logging in with a Google account
AUTH_TYPE=oauth-personal

# Path to OAuth credentials (only for AUTH_TYPE='oauth-personal')
# Replace 'your_username' with your username
GOOGLE_APPLICATION_CREDENTIALS=C:\Users\your_username\.gemini\oauth_creds.json

# Gemini API key (only for AUTH_TYPE='gemini-api-key')
# Get it from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# Template to use - 'gemini-2.5-flash' or 'gemini-2.5-pro'
# Leave empty to use the default CLI template
MODEL=gemini-2.5-flash
