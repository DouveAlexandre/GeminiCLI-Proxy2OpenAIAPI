# Porta onde o servidor irá rodar
PORT=11434

# Tipo de autenticação - pode ser 'oauth-personal', 'gemini-api-key', ou 'vertex-ai'
# Use oauth-personal para acesso gratuito ao Gemini 2.5 Pro fazendo login em uma conta Google
AUTH_TYPE=oauth-personal

# Caminho para as credenciais OAuth (apenas para AUTH_TYPE='oauth-personal')
# Substitua 'seu_usuario' pelo seu nome de usuário
GOOGLE_APPLICATION_CREDENTIALS=C:\Users\seu_usuario\.gemini\oauth_creds.json

# Chave da API Gemini (apenas para AUTH_TYPE='gemini-api-key')
# Obtenha em: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# Modelo a ser usado - 'gemini-2.5-flash' ou 'gemini-2.5-pro'
# Deixe vazio para usar o modelo padrão do CLI
MODEL=gemini-2.5-flash